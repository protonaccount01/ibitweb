{
  "name": "ibit-website",
  "private": true,
  "version": "1.0.0",
  "description": "IBIT Sylhet website — static site + Cloudflare Pages Functions. Tailwind CSS is compiled at build time (CLI), not loaded from the CDN script.",
  "scripts": {
    "build": "tailwindcss -i ./assets/css/tailwind-input.css -o ./assets/css/tailwind.css --minify",
    "watch": "tailwindcss -i ./assets/css/tailwind-input.css -o ./assets/css/tailwind.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.13"
  }
}
